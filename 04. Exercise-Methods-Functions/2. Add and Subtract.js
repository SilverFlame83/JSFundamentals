function addAndSub(x, y, z) {

    // function sum(a, b) {
    //     return a + b;
    // }

    // return sum(x, y)- z;
    let sum = (a, b) => a + b;
    return sum(x, y) - z;
}

console.log(addAndSub(23, 6, 10));

